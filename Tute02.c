
#include <stdio.h>

int main() {
int distance;
float amount;

printf("Distance :");
scanf("%d",&distance);
printf("Amount :");
scanf("%f",&amount);

if (distance => 20)
  amount=30*50;

else (distance => 30)
  amount=30*50+(50-30)*40;
  
  return 0;
}
